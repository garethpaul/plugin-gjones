#!/usr/bin/env node
require('../src/js-yaml-compat');

// Load oclif first so this plugin remains the parent command package.
const { Errors, flush, run } = require('@oclif/core');

require('@twilio/cli-core').configureEnv();
run()
  .then(flush)
  .catch(Errors.handle);
